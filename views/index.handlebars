<div class="container">
    <div class="row">
        <div class="col s12 m12 l12">
            {{#each burgers}}
            <li>Id: {{id}}</li>
            <li>Burger: {{burger_name}}</li>
            {{/each}}
        </div>
    </div>
</div>